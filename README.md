# turboquant-tools

**CLI + Python Library for TurboQuant-based embedding compression.**

Reduce your vector storage by **5x** with near-lossless quality.

```
pip install turboquant-tools
turboquant compress embeddings.npy --bits 3
```

See README for full documentation.
